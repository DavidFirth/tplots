#' Make a Triangular Frame for a Ternary Plot
#'
#' @description When drawn in a square viewport with x and y coordinates in the same units, the result is an equilateral triangle with one of its vertices at (0, 0).
#'
#' @param frame_gp graphical settings via [grid::gpar()]
#' @param ... arguments to pass to [grid::grid.polygon()]
#' 
#' @return A polygon grob object.
#'
#' @details The viewport should be square when the grob gets drawn.  This is not checked.
#'
#' @author David Firth
#'
#' @examples
#' tplot_new(corner = c(1, 1))
#' grid::grid.draw(ternary_frame())
#' #  Now draw another one, scaled down by factor 1 / 1.2
#' grid::pushViewport(ternary_viewport(corner = c(1, 1), scale = 1.2))
#' grid::grid.draw(ternary_frame(gp = grid::gpar(lty = "dashed")))
#' 
#' @export
#' 
ternary_frame <- function(gp = grid::gpar(), ...) {
    xy <- ternary_xy(diag(3),
                     neg_action = NULL)
    grid::polygonGrob(x = xy$x,
                      y = xy$y,
                      gp = gp,
                      ...)
}


